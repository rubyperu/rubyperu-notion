<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Nuevo Documento</h1>

  <%= render "form", document: @document, workspace: @document.workspace %>

  <%= link_to 'Regresar a la lista de documentos', workspace_path(id: params[:workspace_id]), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
